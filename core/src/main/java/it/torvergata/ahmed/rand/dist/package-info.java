/**
 * This package contains implementations of various statistical distributions for random number generation.
 * <p>
 * The package provides a collection of distribution classes that work with the random number generators
 * defined in the parent package. Each distribution implements the {@link it.uniroma2.ahmed.rand.dist.Distribution} interface and
 * provides methods to generate random numbers following specific probability distributions.
 * </p>
 * <p>
 * The distributions can be used with both single-stream ({@link it.torvergata.ahmed.rand.Random})
 * and multi-stream ({@link it.torvergata.ahmed.rand.MultiRandomStream}) random number generators.
 * </p>
 */
package it.torvergata.ahmed.rand.dist;